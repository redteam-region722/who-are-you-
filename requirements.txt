# Core dependencies
cryptography>=41.0.0
pyOpenSSL>=24.0.0
Pillow>=10.0.0
mss>=9.0.0
numpy>=1.24.0
setproctitle>=1.3.0
pynput>=1.7.6
pyperclip>=1.8.2

# Platform-specific dependencies
# Windows service support
pywin32>=306; sys_platform == 'win32'

# Network and compression
aiofiles>=23.0.0

# Webcam support (optional)
opencv-python-headless>=4.8.0

# Web server
Flask>=3.0.0
flask-socketio>=5.3.0
eventlet>=0.33.0

# Wayland support (optional - for Linux Wayland sessions)
# Install with: pip install pyscreenshot
# pyscreenshot>=3.0.0; sys_platform == 'linux'

# Note: macOS and Linux use built-in service managers (launchd/systemd)
# No additional Python packages required for service installation
# pyscreenshot is optional - mss works on X11/Windows/macOS
# Install pyscreenshot for Wayland support on Linux
